# GitLab Release Configuration
# Copy this file to .gitlab-release.env and fill in your values
# DO NOT commit .gitlab-release.env to git (it's in .gitignore)

# GitLab Personal Access Token
# Get it from: GitLab → Settings → Access Tokens
# Required scopes: api
GITLAB_TOKEN="your_token_here"

# GitLab API URL (optional, auto-detected from remote URL)
# For GitLab.com: https://gitlab.com/api/v4
# For self-hosted: https://your-gitlab-domain.com/api/v4
GITLAB_API_URL="https://gitlab.com/api/v4"

